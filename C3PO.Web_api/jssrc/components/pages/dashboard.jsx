import { connect } from 'react-redux';
import React, { Component } from 'react';

class Dashboard extends Component {
    render() {
        return (
            <div>Dashboard</div>
        );
    }
}

export default Dashboard;
//export default connect((state) => { return { data: state }; })(Dashboard);